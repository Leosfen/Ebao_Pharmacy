<resources>

    <!-- Application theme. -->
    <style name="CustomAppTheme" parent="android:Theme.Light">
        <item name="android:windowBackground">@color/bg_coclor</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
