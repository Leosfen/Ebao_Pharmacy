<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        android:id="@+id/refresh_des"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/refresh_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="下拉刷新"
            android:textColor="@color/color_black_484747"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/refresh_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="5分钟前更新"
            android:visibility="gone" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/refresh_progress"
        android:layout_width="21dip"
        android:layout_height="21dip"
        android:layout_centerInParent="true"
        android:layout_marginRight="20dip"
        android:indeterminateDrawable="@drawable/pharmacy_loading_small_anim"
        android:visibility="invisible" />

    <ImageView
        android:id="@+id/refresh_arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginRight="10dip"
        android:layout_toLeftOf="@id/refresh_des"
        android:src="@drawable/pharmacy_refresh_icon_arrow" />

</RelativeLayout>